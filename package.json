{
  "name": "fredux",
  "version": "0.0.1",
  "description": "Redux over Firebase.",
  "main": "index.js",
  "scripts": {
    "clean": "rm ./dist -r",
    "build": "npm run clean && tsc --outDir dist && cp -R ./spec/support/ dist/spec/",
    "test": "npm run build && cd ./dist && jasmine",
    "publish": "npm run clean && tsc --outFile dist/lib.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/cors": "^2.8.1",
    "cors": "^2.8.3",
    "firebase": "^4.1.2",
    "firebase-admin": "^5.0.0",
    "firebase-functions": "^0.5.7"
  },
  "devDependencies": {
    "@types/jasmine": "^2.5.51",
    "jasmine": "^2.6.0"
  }
}
